# Welcome to RideRank

App tracks taxi rides and presents simple statistics.

## Requirements

* Ruby on Rails 5
* PostgreSQL

## Install

* clone repository
* bundle install
* rake db:create
* rake db:migrate
* rake db:seed

## Run tests
* bundle exec rspec
* bundle exec guard
